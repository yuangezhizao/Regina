name: Build ESP-IDF project

on:
  workflow_dispatch:
  push:
    branches:
      - dev

env:
  ESP_IDF_TARGET: ESP32C6
  ESP_IDF_PROJECT_PATH: platforms/Regina
  ESP_IDF_COMPONENTS_PATH: dependencies
  ARTIFACT_BOOTLOADER_BIN_PATH: build/bootloader/bootloader.bin
  ARTIFACT_PARTITION_TABLE_BIN_PATH: build/partition_table/partition-table.bin
  ARTIFACT_APP_BIN_PATH: build/Regina.bin

jobs:
  build:
    continue-on-error: ${{ matrix.experimental }}

    strategy:
      fail-fast: true
      matrix:
        os:
          - ubuntu-latest
        esp_idf_version:
          - release-v5.5
          - release-v5.4
          - release-v5.3
        experimental:
          - false
        include:
          - os: ubuntu-latest
            esp_idf_version: latest
            experimental: true

    runs-on: ${{ matrix.os }}

    steps:
      - name: 📤 Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: 📥 Fetch dependencies
        run: |
          mkdir -pv ${{ env.ESP_IDF_COMPONENTS_PATH }}
          python3 -V
          python3 fetch_repos.py

      - name: 🛠️ Build ESP-IDF project
        uses: espressif/esp-idf-ci-action@v1
        with:
          esp_idf_version: ${{ matrix.esp_idf_version }}
          target: ${{ env.ESP_IDF_TARGET }}
          path: ${{ env.ESP_IDF_PROJECT_PATH }}

      - name: 📏 Show size by idf.py
        uses: espressif/esp-idf-ci-action@v1
        with:
          esp_idf_version: ${{ matrix.esp_idf_version }}
          target: ${{ env.ESP_IDF_TARGET }}
          path: ${{ env.ESP_IDF_PROJECT_PATH }}
          command: |
            idf.py size
            idf.py size-components
            idf.py size-files

      - name: ℹ️ Show image_info by esptool.py
        uses: espressif/esp-idf-ci-action@v1
        with:
          esp_idf_version: ${{ matrix.esp_idf_version }}
          target: ${{ env.ESP_IDF_TARGET }}
          path: ${{ env.ESP_IDF_PROJECT_PATH }}
          command: |
            esptool.py image_info ${{ env.ARTIFACT_BOOTLOADER_BIN_PATH }} --version 2
            echo -e
            esptool.py image_info ${{ env.ARTIFACT_APP_BIN_PATH }} --version 2

      - name: 📦 Upload build artifacts (include binary images)
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ESP_IDF_TARGET }}-build-artifacts_on-${{ matrix.os }}_with-esp-idf-${{ matrix.esp_idf_version }}
          path: |
            ${{ env.ESP_IDF_PROJECT_PATH }}/build
            # ${{ env.ESP_IDF_PROJECT_PATH }}/${{ env.ARTIFACT_BOOTLOADER_BIN_PATH }}
            # ${{ env.ESP_IDF_PROJECT_PATH }}/${{ env.ARTIFACT_PARTITION_TABLE_BIN_PATH }}
            # ${{ env.ESP_IDF_PROJECT_PATH }}/${{ env.ARTIFACT_APP_BIN_PATH }}
